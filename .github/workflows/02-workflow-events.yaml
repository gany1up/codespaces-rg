name: 02 - Workflow Events
on: 
  push:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps: 
      - name: show the trigger
        run: echo "type the name ${{ github.event_name }}"
